<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
              xmlns:app="http://schemas.android.com/apk/res-auto"
              xmlns:tools="http://schemas.android.com/tools"
              style="@style/Widget.WidgetSchedule.AppWidget.Container"
              android:theme="@style/Theme.WidgetSchedule.AppWidgetContainer"
              android:layout_width="match_parent"
              android:layout_height="match_parent"
              android:orientation="vertical"
              android:padding="-16dp">
    <LinearLayout 
            android:layout_width="match_parent" 
            android:layout_height="wrap_content"
            android:background="@drawable/top_rounded_background"
            android:backgroundTint="@color/dark_gray"
            android:orientation="horizontal"
            >
        <TextView
                android:id="@+id/widget_header"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:gravity="center_vertical"
                android:text="Schedule"
                android:textColor="@color/white"
                android:textStyle="bold"
                android:textSize="16sp"
                android:padding="16dp"
                android:layout_weight="1"/>

        <TextView
                android:id="@+id/widget_current_items_button"
                android:layout_width="wrap_content"
                android:layout_height="match_parent"
                android:layout_weight="0.075"
                android:src="@drawable/round_calendar_today"
                android:text="@string/all"
                android:gravity="center"
                android:textStyle="bold"
                android:layout_margin="2dp"
                android:textColor="@color/white"
                android:backgroundTint="@android:color/transparent"
                android:background="@drawable/circle"
                android:contentDescription="@string/widget_current_items_button"/>

        <ImageButton
                android:id="@+id/widget_refresh_button"
                android:layout_width="wrap_content"
                android:layout_height="match_parent"
                android:layout_weight="0.075"
                android:src="@drawable/round_refresh"
                android:layout_margin="2dp"
                android:textColor="@color/white"
                android:backgroundTint="@android:color/transparent"
                android:background="@drawable/circle"
                android:contentDescription="@string/refresh_the_widget"/>
    </LinearLayout>
    <ListView
            android:id="@+id/calendar_listview"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:divider="@null"
            android:dividerHeight="0dp"
            android:listSelector="@android:color/transparent" />
</LinearLayout>